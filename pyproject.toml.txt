[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mbodi-client"
version = "0.1.0"
description = "HTTPX Python client for Mbodi AI FastAPI inference and data collection service"
readme = "README.md"
requires-python = ">=3.9"
authors = [
  { name = "Neil Sriram" }
]
dependencies = [
  "httpx>=0.27.0",
  "numpy>=1.24.0",
  "pillow>=10.0.0"
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]